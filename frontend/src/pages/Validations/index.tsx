import React from 'react';
import { Box, Typography } from '@mui/material';

export const Validations: React.FC = () => {
  return (
    <Box>
      <Typography variant="h4">Validações</Typography>
    </Box>
  );
};